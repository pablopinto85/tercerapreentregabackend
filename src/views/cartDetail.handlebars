<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles de carrito</title>
</head>
<body>
    <h1>Detalles del Carrito</h1>
    <div>
        
    {{#if cart}}
        <h2>Productos en el carrito:</h2>
        <ul>
            {{#each cart.products}}
                <li>{{this.title}} - Precio: {{this.price}}</li>
            {{/each}}
        </ul>
    {{else}}
        <p>No se encontró el carrito.</p>
    {{/if}}
    </div>
    
    {{!-- AGREGAR UN BOTON O UN LINK PARA VOLVER A VER TODOS LOS PRODUCTOS PERO DESPUES PORQUE NO LO PIDE LA TAREA --}}

</body>
</html>